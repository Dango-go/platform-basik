# Workspace Rules & Knowledge Pointer

> **Primary Memory Document**: Read [PROJECT_MEMORY.md](file:///home/bodya/Projects/db-idp/PROJECT_MEMORY.md) at the start of any conversation in this workspace.

## Core Rules & Directives:
1. **Language Preference**: Respond in Ukrainian (Українська).
2. **Browser Preference**: Never open browser windows or invoke `browser_subagent` automatically unless explicitly asked by the user.
3. **Architecture Style**: Keep microservices stateless where possible (`operator-service`, `helm-deployer`).
4. **Header Titles**: DB Management Console header title MUST be `"Management Database Console"`.
5. **No Preset Deployment Mode**: Preset deployment mode has been completely removed.
